# Paginação
webpages.pagesize=1

# Caminho do arquivo de configuracoes
pathToConfigFile=classpath:

# Configuracoes de log
pathToLogFile=./logs
classesLogLevel=TRACE
sqlLogLevel=TRACE

# Configuracoes de conexao com o banco de dados
jdbc.url=jdbc:mysql://localhost:3306/uaijugvagas
jdbc.username=root
jdbc.password=leopardo2014
jdbc.driver_className=com.mysql.jdbc.Driver

# Configurações do JPA
jpa.generateDdl=true
jpa.persistence.dialect=org.hibernate.dialect.MySQLDialect

# Configuracoes Hibernate e C3P0
hibernate.show_sql=true
hibernate.c3p0.minPoolSize=5
hibernate.c3p0.maxPoolSize=30
hibernate.c3p0.maxStatements=0
hibernate.c3p0.initialPoolSize=3
hibernate.c3p0.acquireIncrement=3

# Configurações de email
email.username=hercules@jnaldo.com
email.password=mL5zTtg9P2st
email.smtp.auth=true
email.smtp.starttls.enable=true
email.smtp.host=smtp.gmail.com
email.smtp.port=587
email.charset=UTF-8
email.text.type=text/html

# Aplicação
application.url=https://localhost:8080
# Url de Login
hamaka.login.url=https://localhost:8080